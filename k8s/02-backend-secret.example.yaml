# TEMPLATE - copy this file to "02-backend-secret.yaml" and fill in real
# values before applying. Never commit the real secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: backend-secrets
  namespace: skinscan
type: Opaque
stringData:
  SECRET_KEY: "change-me-to-a-random-secret-string"
  DB_PASSWORD: "change-me-too"
  GEMINI_API_KEY: ""
