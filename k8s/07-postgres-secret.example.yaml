# TEMPLATE - copy to "07-postgres-secret.yaml" and fill in a real value
# before applying. Never commit the real secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: skinscan
type: Opaque
stringData:
  POSTGRES_PASSWORD: "change-me-too"
