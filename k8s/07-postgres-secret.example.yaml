# TEMPLATE - copy to "07-postgres-secret.yaml" (gitignored, see
# .gitignore's "k8s/*-secret.yaml") and fill in a real value before
# applying. Never commit the real secret.yaml.
apiVersion: v1
kind: Secret
metadata:
  name: postgres-secrets
  namespace: skinscan
type: Opaque
stringData:
  # Must match DB_PASSWORD in 02-backend-secret.yaml exactly - this is
  # the password Postgres is actually initialized with; the backend's
  # copy is just what it uses to connect.
  POSTGRES_PASSWORD: "change-me-too"
